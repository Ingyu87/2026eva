# AGENTS.md

AI 에이전트가 이 저장소에서 작업할 때 **가장 먼저 읽는 파일**입니다.
Codex, Claude Code 등 도구와 무관하게 같은 규칙을 따릅니다.

---

## 0. 30초 요약

서울특별시교육청 학교평가를 돕는 Next.js 웹앱입니다. 산출물이 **교육지원청에 제출**되므로
규정 적합성이 기능보다 우선합니다.

```bash
npm install
npm run verify      # typecheck + 문항 풀 + 디자인 규칙
npm run dev         # http://localhost:3000
```

**현재 5단계(구글폼 확장) 완료(실사용 확인 대기), 6단계(계산 엔진) API 완료. S4 화면 UI가 다음.**
진행 상황은 [docs/implementation.md](docs/implementation.md)의 표에서 확인하세요.

---

## 1. 무엇을 만들고 있나

1. 교육청 예시자료에서 평가문항을 골라 설문을 만든다 ✅
2. DOCX 설문지와 Google Forms로 내보낸다 ✅
3. **설문 결과를 넣으면 분석해 제출 서류 3종을 만든다** ← 지금 만드는 중

산출물 3종은 양식을 그대로 지켜야 합니다.

| 산출물 | 형식 | 기준 |
|---|---|---|
| 평가지표 및 현황 | XLSX | 교육청 배포 템플릿에 값만 주입 |
| 설문 결과 보고서 | HTML | 탭 3개(척도/선택형/서술형) |
| 학교평가서 | DOCX | 가이드북 서식3-1 · 3-2 |

---

## 2. 문서 지도

작업 전에 해당 문서를 읽으세요. 코드만 보고 고치면 규정을 어깁니다.

| 문서 | 언제 읽나 |
|---|---|
| [docs/README.md](docs/README.md) | 문서 4종이 각각 무슨 역할인지 |
| [docs/prd.md](docs/prd.md) | 왜 만드는가, **무엇을 안 만드는가**(비목표) |
| [docs/spec.md](docs/spec.md) | 화면·데이터·API·계산식 — **구현 전 필독** |
| [docs/design.md](docs/design.md) | 색·글꼴·간격·부품 규격 |
| [docs/implementation.md](docs/implementation.md) | 단계별 계획, 진행 상황, 환경 주의사항 |

"이것도 넣을까요?" 싶으면 `docs/prd.md` 4장 비목표를 먼저 보세요.

---

## 3. 절대 어기면 안 되는 것 6가지

아래를 어기면 **잘못된 서류가 교육지원청에 제출됩니다.**

### 3.1 영역·세부영역은 학교가 수정할 수 없습니다

법령과 교육청 정책으로 정해진 값입니다 (기본계획 Ⅴ-3-가-2, 가이드북 Q6).
평가지표와 평가문항만 학교가 자율로 구성합니다.

기준값은 **`src/lib/evaluationFramework.ts` 한 곳에만** 둡니다.
다른 파일에 세부영역 목록을 복사하지 마세요.

### 3.2 점수 계산을 LLM에 맡기지 마세요

가이드북 p.51 공식을 코드로 구현합니다.

```
문항 평균 = Σ(응답자 수 × 척도값) / 문항 전체 응답자 수
영역 평균 = Σ(영역 응답 소계 × 척도값) / 영역 문항 응답 합계
```

**영역 평균은 문항 평균의 단순 평균이 아닙니다.** 영역 전체 응답 기준 가중평균입니다.
흔히 틀리는 지점이니 단위 테스트로 고정하세요.

검증 케이스(가이드북 51쪽 예시):
```
5점 113명, 4점 35명, 3점 16명, 2점 7명, 1점 4명 (합 175)
→ 771 / 175 = 4.405 → 4.4 → "매우 우수"
```

LLM(Gemini)은 **계산된 숫자를 해석**하는 데만 씁니다.

### 3.3 척도는 라벨로 매핑합니다

`"매우 그렇다" = 5`. 순서나 인덱스로 계산하지 마세요.

저장소의 옛 설문 JSON은 `value 1 = 매우 그렇다`로 **뒤집혀** 있습니다.
인덱스 기반으로 짜면 4.9점이 최악 평가가 됩니다.

### 3.4 5점 척도가 아닌 문항은 영역 평균에 넣지 마세요

가이드북 p.9·p.51이 정량평가를 5단계 척도로 전제합니다.
`countsTowardAreaMean()` (`src/lib/types.ts`)로 판단하세요.

서술형·객관식은 정성평가로 병행하되 집계와 분리합니다.

### 3.5 중간평가 결과로 제출 서류를 만들지 마세요

가이드북 Q12: *"학교평가서에는 학년말 최종 설문 평가 결과를 반영하도록 합니다."*

판단은 **`src/lib/exportGate.ts`에서만** 합니다. 화면마다 따로 판단하면
한쪽만 고쳐져 어긋납니다.

### 3.6 저장할 때 남의 작업을 덮어쓰지 마세요

여러 부장이 **같은 학교 계정으로 동시에** 작업합니다. 자세한 내용은 4장.

---

## 4. 동시 편집 — 되돌리면 유실이 재발합니다

예전에 부장들의 작업이 서로 사라지던 문제가 있었고, 구조를 바꿔 고쳤습니다.
"코드를 단순하게 정리"하다 아래를 되돌리면 같은 문제가 그대로 재발합니다.

| 지켜야 할 것 | 되돌리면 |
|---|---|
| 문항은 `surveyDrafts/{id}/items/{itemId}` **문서 하나씩** | 대상별 배열로 합치면 문항 하나 고칠 때 4개 대상을 통째로 덮어씀 |
| `order`는 **분수 인덱스** | 1,2,3 재번호를 매기면 한 명이 순서 바꿀 때마다 전 항목 충돌 |
| 쓰기는 `expectedRev` + 트랜잭션 | 검사를 빼면 나중 저장자가 앞사람 작업을 말없이 지움 |
| 충돌 시 **409 + 최신 내용 반환**, 사용자가 선택 | 자동 병합·자동 덮어쓰기를 넣지 마세요 |
| 동기화 커서는 **서버 타임스탬프** | `rev`는 문서마다 따로 도는 값이라 커서로 못 씁니다 |
| 삭제는 **tombstone**(`deleted: true`) | 문서를 지우면 상대 화면에 삭제가 전달되지 않음 |
| 편집은 **localStorage 큐에 먼저** 기록 | 네트워크가 끊기면 작업이 사라짐 (학교망에서 실제로 발생) |

관련 파일:
```
src/lib/store.ts               서브컬렉션·트랜잭션·마이그레이션
src/lib/order.ts               분수 인덱스
src/lib/outbox.ts              로컬 큐
src/hooks/useDraftWorkspace.ts 저장·폴링·충돌·접속자
app/api/draft/                 부분 저장 API
```

**저장 관련 코드를 고쳤으면 `npm run verify:concurrent`를 반드시 통과시키세요.**

---

## 5. 화면 규칙

- **페이지는 세로로 스크롤하지 않습니다.** 24인치 한 화면에 다 들어갑니다.
  높이를 픽셀로 고정하지 마세요. 안쪽 열만 스크롤합니다.
- 색은 `app/styles/tokens.css`에 있는 것만. 새 색은 토큰에 먼저 추가하세요.
- 간격은 4의 배수만.
- 강조색은 남보라 하나. 초록·주황·빨강은 상태 표시 전용.
- 사이드바·전체메뉴·브레드크럼·FAB는 **일부러 뺐습니다.** 화면이 6개뿐이고
  3열 작업화면에 가로 폭을 다 써야 합니다. 다시 넣지 마세요.

`npm run verify:design`이 위 항목을 기계적으로 검사합니다.

### 편집 UI 주의

`onBlur`로 편집 영역을 닫지 마세요. 드롭다운을 누르는 순간 닫혀서
유형을 고를 수 없게 됩니다. 실제로 그 버그가 있었습니다.
명시적인 "완료" 버튼이나 Esc로 닫습니다.

---

## 6. 검증

손으로 확인하지 말고 스크립트를 쓰세요.

```bash
npm run verify              # typecheck + bank + design

# 동시 편집 검증은 서버가 필요합니다
npm run dev &
BASE_URL=http://127.0.0.1:3000 bash scripts/verify-concurrent-editing.sh

# 또는 프로덕션 빌드로
npm run build && npx next start -p 3100 &
npm run verify:concurrent
```

| 스크립트 | 무엇을 보장하나 | 현재 |
|---|---|---|
| `verify:bank` | 문항 풀이 2026 평가체제를 지키는가 | 8/8 |
| `verify:design` | 색·간격·높이 고정 규칙 | 6/6 |
| `verify:concurrent` | **두 사람이 동시에 작업해도 유실이 없는가** | 22/22 |

`verify:concurrent`가 이 저장소에서 가장 중요합니다.

> ⚠️ **dev 서버를 켠 채로 `npm run build`를 돌리지 마세요.**
> `.next`를 함께 쓰다 깨져서 500이 납니다. 깨지면 서버를 끄고
> `rm -rf .next` 후 다시 띄우세요.

---

## 7. 이 환경에서 막혀 있던 것

개발하던 PC(학교망)에서 막혔던 것들입니다. **다른 PC에서는 안 막힐 수 있습니다.**

| 막힌 것 | 그래서 한 일 |
|---|---|
| Google Fonts 접속 | `next/font/google` 제거. **다시 넣으면 빌드가 통째로 실패합니다.** 글꼴은 `tokens.css`의 `--font-sans`(Pretendard → 맑은 고딕)로 정합니다 |
| Playwright 크로미움 다운로드 | 화면 확인을 자동화하지 못했습니다. **사람이 브라우저로 직접 봐야 합니다** |

막히지 않는 PC라면 Playwright로 스크린샷 검증을 추가하면 좋습니다.
2단계 이후 화면을 육안으로 충분히 확인하지 못한 상태입니다.

---

## 8. 공개 저장소입니다

`github.com/Ingyu87/2026eva` 는 **PUBLIC** 입니다.
학교 실제 데이터를 커밋하지 마세요. `.gitignore`로 막아 두었지만 확인하세요.

- 설문 결과 HTML — **학생·학부모 서술형 응답 원문**이 들어 있습니다
- 학교평가서 PDF, 평가지표 및 현황 XLSX — 학교별 평가 결과

저장소에 없지만 특정 작업에 필요한 파일:

| 파일 | 필요한 단계 |
|---|---|
| `1_(초)2026 … 예시 자료.pdf` | 문항 풀을 다시 만들 때만 (이미 JSON으로 커밋됨) |
| `2025 … 평가지표 및 현황(가동초).xlsx` | 8단계 템플릿 자동 인식 |
| `2025학년도 학교평가서(가동초).pdf` | 7단계 전년도 평가서 추출 |
| `서울가동초 … 설문 결과.html` | 8단계 결과 보고서 형식 대조 |

---

## 9. 다음 작업 — 5단계 구글폼 확장 (코드 완료, 실사용 확인 대기)

`src/lib/googleForms.ts`에 아래가 들어갔습니다.

1. **학년 분류 문항 자동 삽입** — 학생용 폼 맨 앞, 필수.
   `studentGrades`가 비어 있으면 학생용 폼 생성 자체를 막습니다(에러로 안내).
2. **문항 ↔ 구글폼 연결표 저장** — `GoogleFormInfo.questionLinks`/`gradeQuestion`에
   폼 생성 시점의 제목을 스냅숏으로 저장합니다. 문항의 `id`를 그대로 Google Forms
   `item.itemId`로 지정해 두어(Forms API가 생성 시 클라이언트 지정 ID를 허용) 연결이 명시적입니다.
   6단계에서 결과 파일의 열 제목을 이 스냅숏과 대조해 자동 연결합니다.
   사용자가 구글폼에서 문항을 직접 고치면(제목이 스냅숏과 달라짐) 연결이 끊어지므로,
   **수동 연결 화면은 S4(6단계, 결과 분석 화면)에서 만듭니다** — spec.md 9장 참고.

선택형·서술형 문항은 이미 됩니다(`choice_single`, `checklist`, `text`).
보기 목록 편집기는 `src/components/workspace/ResponseTypeEditor.tsx` 에 있습니다.

**남은 것:** 이 개발 환경에는 Google OAuth 크리덴셜이 없어 실제 Forms API 호출을 확인하지
못했습니다(typecheck·verify:bank·verify:design만 통과). 실제 계정으로 학생용 폼을 만들어
① 첫 문항이 학년 선택인지 ② 선택형/서술형이 정상 생성되는지 확인한 뒤 `docs/implementation.md`의
5단계를 완료로 바꾸세요.

그다음은 6단계 계산 엔진 → 7단계 AI 해석 → 8단계 산출물입니다.
각 단계의 "끝났다고 보는 기준"이 `docs/implementation.md`에 체크리스트로 있습니다.
다 만들었으면 그 항목을 채우고 진행 상황 표를 갱신하세요.

---

## 10. 커밋 규칙

- 커밋 메시지는 한국어. **무엇을 왜 바꿨는지** 적습니다.
  규정 근거가 있으면 출처(가이드북 쪽수, 기본계획 절)를 함께 씁니다.
- 검증을 돌렸으면 결과를 메시지에 남깁니다.
- 학교 데이터가 섞이지 않았는지 `git status`로 확인하고 커밋합니다.
