# 학교평가 웹앱 PRD

## 1. 문서 정보

- 문서명: 학교평가 설문 생성 웹앱 PRD
- 작성일: 2026-05-07
- 배포 목표: GitHub 저장소 연동 후 Vercel 배포
- 데이터베이스: Firebase Firestore
- 주요 입력 자료:
  - `(초등학교)_2025 학교평가 평가문항 예시 자료.xlsx`
  - `json_export/학생용.json`
  - `json_export/학부모용.json`
  - `json_export/교원용.json`
  - `json_export/직원용.json`
  - `2026학년도 1학기 학교교육과정 운영 평가 설문.pdf`

## 2. 제품 개요

학교별로 학교평가 설문 문항을 선택, 수정, 저장하고, 최종 설문지를 DOCX 및 Google Forms로 생성하는 웹앱을 만든다.

앱은 관리자용과 사용자용으로 나뉜다. 사용자는 학교 이름과 비밀번호를 등록해 학교 계정을 만들고, 로그인 후 대상별 문항 풀에서 필요한 평가문항을 선택한다. 선택한 문항은 편집 가능하며, 최종 출력 시 PDF 예시 문서와 유사한 형식의 DOCX 파일을 생성하고 Google Forms도 함께 생성한다.

## 3. 목표

- 학교별 설문 문항 구성 작업을 웹에서 완료할 수 있게 한다.
- 기존 Excel 문항 풀을 `교원용`, `학부모용`, `학생용`, `교직원용` 탭으로 탐색하게 한다.
- 영역, 세부영역, 평가지표 선택에 따라 관련 평가문항만 보여준다.
- 사용자가 선택한 문항을 설문지에 담고, 문항 텍스트를 수정할 수 있게 한다.
- 최종 설문지를 DOCX 파일로 다운로드한다.
- 사용자의 Google 계정 OAuth 동의를 받아 Google Forms를 자동 생성한다.
- 관리자는 학교 계정 목록과 비밀번호 초기화를 관리한다.

## 4. 비목표

- 실제 설문 응답 수집 및 통계 분석은 1차 범위에서 제외한다.
- Google Forms 응답 결과를 다시 앱으로 가져오는 기능은 1차 범위에서 제외한다.
- 다중 관리자 계정, 관리자 권한별 세분화는 1차 범위에서 제외한다.
- HWP 출력은 1차 범위에서 제외하고 DOCX만 지원한다.

## 5. 사용자 유형

### 5.1 학교 사용자

- 학교 이름과 비밀번호로 등록 및 로그인한다.
- 문항 풀을 탐색하고 설문 문항을 선택한다.
- 선택한 문항을 학교 상황에 맞게 수정한다.
- DOCX 설문지를 출력한다.
- Google 로그인 후 Google Forms를 생성한다.

### 5.2 관리자

- 관리자 비밀번호로 관리자 화면에 접근한다.
- 등록된 학교 목록을 확인한다.
- 학교 계정의 비밀번호를 초기화한다.
- 학교 계정을 삭제하거나 비활성화할 수 있다.

관리자 비밀번호는 MVP 단계에서 `백인규`로 고정한다. 단, Vercel 배포 시 코드에 직접 하드코딩하지 않고 환경변수 `ADMIN_PASSWORD`에 저장하는 것을 권장한다.

## 6. 핵심 사용자 흐름

### 6.1 학교 계정 등록

1. 사용자가 사용자용 화면에서 `학교 등록`을 선택한다.
2. 학교 이름과 비밀번호를 입력한다.
3. 시스템은 같은 학교 이름이 이미 등록되어 있는지 확인한다.
4. 중복이 없으면 비밀번호를 해시 처리해 Firebase에 저장한다.
5. 등록 완료 후 사용자 대시보드로 이동한다.

### 6.2 학교 사용자 로그인

1. 사용자가 학교 이름과 비밀번호를 입력한다.
2. 시스템은 Firebase의 학교 계정과 비밀번호 해시를 검증한다.
3. 성공 시 학교별 작업 공간으로 이동한다.
4. 실패 시 오류 메시지를 표시한다.

### 6.3 문항 선택 및 설문 구성

1. 사용자는 `교원용`, `학부모용`, `학생용`, `교직원용` 탭 중 하나를 선택한다.
2. 해당 탭에서 `영역`, `세부영역`, `평가지표`를 순서대로 선택한다.
3. 시스템은 선택 조건과 일치하는 평가문항 목록을 보여준다.
4. 사용자가 평가문항을 클릭하면 해당 문항이 선택 문항 목록에 담긴다.
5. 사용자는 선택 문항 목록에서 문항 순서를 변경하거나 삭제할 수 있다.
6. 사용자는 선택된 문항 텍스트를 직접 수정할 수 있다.
7. 시스템은 변경 내용을 학교별 설문 초안으로 자동 저장한다.

### 6.4 DOCX 출력

1. 사용자가 `출력` 버튼을 누른다.
2. 시스템은 대상별 선택 문항을 모아 설문 문서를 생성한다.
3. 문서 형식은 `2026학년도 1학기 학교교육과정 운영 평가 설문.pdf`와 유사해야 한다.
4. 생성된 DOCX 파일을 다운로드한다.

### 6.5 Google Forms 생성

1. 사용자가 `Google Forms 만들기` 버튼을 누른다.
2. Google OAuth 로그인 및 권한 동의 화면으로 이동한다.
3. 권한 동의 후 앱으로 돌아온다.
4. 시스템은 Google Forms API로 빈 Form을 만든다.
5. 선택된 문항들을 Form 문항으로 추가한다.
6. 생성 완료 후 Google Forms 편집 링크와 응답 링크를 보여준다.

### 6.6 관리자 관리

1. 관리자가 관리자 화면에서 비밀번호 `백인규`를 입력한다.
2. 등록된 학교 목록을 조회한다.
3. 학교별 등록일, 최근 수정일, 설문 초안 수, Google Forms 생성 여부를 확인한다.
4. 특정 학교의 비밀번호를 새 비밀번호로 초기화한다.
5. 필요 시 학교 계정을 비활성화하거나 삭제한다.

## 7. 기능 요구사항

### 7.1 사용자/관리자 분리

- 첫 화면은 `사용자용`과 `관리자용` 진입 버튼을 제공한다.
- 사용자용은 학교 등록, 로그인, 설문 편집 기능을 제공한다.
- 관리자용은 학교 계정 관리 기능만 제공한다.

### 7.2 학교 계정

- 필수 입력값:
  - 학교 이름
  - 비밀번호
- 학교 이름은 중복 등록할 수 없다.
- 비밀번호는 원문 저장을 금지하고 해시로 저장한다.
- 로그인 세션은 HttpOnly 쿠키 또는 Firebase 기반 세션으로 유지한다.

### 7.3 문항 풀

- 앱 초기 문항 풀은 `json_export` 폴더의 JSON 데이터를 사용한다.
- Excel의 `직원용.json`은 앱 UI에서 `교직원용` 탭으로 표시한다.
- 각 문항은 다음 필드를 가진다.
  - 원본행
  - 영역
  - 세부영역
  - 평가지표
  - 평가문항
- 문항 풀은 읽기 전용 기본 데이터로 취급한다.
- 사용자가 수정한 문항은 학교별 설문 초안에만 저장한다.

### 7.4 필터링

- 탭 선택 후 해당 탭의 고유 `영역` 목록을 보여준다.
- 영역 선택 후 해당 영역의 `세부영역` 목록을 보여준다.
- 세부영역 선택 후 해당 세부영역의 `평가지표` 목록을 보여준다.
- 평가지표 선택 후 관련 평가문항 목록을 보여준다.
- 필터는 선택 해제 및 초기화할 수 있어야 한다.

### 7.5 문항 담기

- 평가문항 목록에서 문항을 클릭하면 선택 문항 목록에 추가된다.
- 이미 담긴 문항은 중복 추가를 방지하거나 사용자에게 확인을 받아야 한다.
- 선택 문항 목록은 대상 탭별로 독립 관리한다.
- 선택 문항에는 다음 값이 저장된다.
  - 원본 문항 ID
  - 대상 탭
  - 영역
  - 세부영역
  - 평가지표
  - 원문 평가문항
  - 수정 평가문항
  - 순서
  - 응답 유형

### 7.6 문항 수정

- 선택 문항의 텍스트를 인라인 또는 모달에서 수정할 수 있다.
- 수정 전 원문과 수정 후 문항을 모두 저장한다.
- 수정 이력은 1차 범위에서는 필수는 아니지만, 마지막 수정일은 저장한다.

### 7.7 응답 유형

- 기본 응답 유형은 5점 척도이다.
- 5점 척도 문구:
  - 매우 그렇다
  - 그렇다
  - 보통이다
  - 그렇지 않다
  - 전혀 그렇지 않다
- 사용자는 문항별로 `5점 척도` 또는 `서술형`을 선택할 수 있다.
- Google Forms 생성 시 5점 척도는 객관식 또는 선형 배율 중 하나로 매핑한다.
- MVP 기본값은 객관식 5개 선택지로 한다.

### 7.8 설문 메타데이터

- 학교별 설문 초안은 다음 메타데이터를 가진다.
  - 설문 제목
  - 학교명
  - 작성 기준일
  - 안내 문구
  - 대상별 섹션 사용 여부
- 기본 제목 예시:
  - `2026학년도 1학기 학교교육과정 운영 평가 설문`
- 기본 안내 문구는 PDF 예시를 참고해 대상별로 제공하되, 사용자가 수정할 수 있게 한다.

### 7.9 DOCX 생성

- DOCX는 서버 사이드에서 생성한다.
- 출력 문서는 대상별 설문지를 한 파일에 포함한다.
- 대상별 섹션 순서 기본값:
  1. 교원용
  2. 학부모용
  3. 학생용
  4. 교직원용
- 각 섹션은 제목, 안내문, 날짜, 학교명, 문항 표를 포함한다.
- 문항 표는 `번호`, `문항` 컬럼으로 구성한다.
- 5점 척도 문항은 문항 아래에 선택지를 표시한다.
- 서술형 문항은 답변 작성 공간을 표시한다.

### 7.10 Google Forms 생성

- Google Forms 생성은 사용자의 Google 계정 OAuth 동의가 필요하다.
- Google Forms API 사용 흐름:
  1. `forms.create`로 제목이 있는 빈 Form 생성
  2. `forms.batchUpdate`로 섹션 및 문항 추가
- 필요한 OAuth scope는 최소 범위 우선으로 검토한다.
  - `https://www.googleapis.com/auth/forms.body`
  - 필요 시 `https://www.googleapis.com/auth/drive.file`
- 생성 결과는 학교별 초안에 저장한다.
  - formId
  - editUrl
  - responderUrl
  - 생성일
  - 생성한 Google 계정 이메일
- OAuth refresh token 저장 여부는 보안 검토 후 결정한다.
- MVP에서는 Google Forms 생성 시점에만 access token을 사용하고, 장기 보관은 하지 않는 방향을 우선한다.

### 7.11 관리자 기능

- 학교 목록 조회
- 학교 검색
- 학교별 비밀번호 초기화
- 학교 계정 비활성화
- 학교 계정 삭제
- 학교별 생성된 설문 초안 요약 조회
- 관리자 작업 결과 토스트 또는 알림 표시

## 8. 화면 요구사항

### 8.1 첫 화면

- 서비스명
- 사용자용 진입 버튼
- 관리자용 진입 버튼

### 8.2 사용자 등록/로그인 화면

- 학교 이름 입력
- 비밀번호 입력
- 학교 등록 버튼
- 로그인 버튼
- 오류 및 성공 메시지

### 8.3 사용자 대시보드

- 현재 로그인한 학교명
- 설문 제목/날짜/안내문 설정 영역
- 대상 탭:
  - 교원용
  - 학부모용
  - 학생용
  - 교직원용
- 필터 영역:
  - 영역
  - 세부영역
  - 평가지표
- 평가문항 목록
- 선택 문항 목록
- 저장 상태 표시
- DOCX 출력 버튼
- Google Forms 만들기 버튼

### 8.4 문항 편집 화면

- 선택 문항 전체 목록
- 문항별 대상, 영역, 세부영역, 평가지표 표시
- 문항 텍스트 수정
- 응답 유형 선택
- 순서 변경
- 삭제

### 8.5 관리자 화면

- 관리자 비밀번호 입력
- 등록 학교 목록
- 학교별 상태 표시
- 비밀번호 초기화
- 비활성화/삭제

## 9. 데이터 모델 초안

### 9.1 `schools`

```json
{
  "id": "auto-id",
  "schoolName": "서울가동초등학교",
  "schoolNameNormalized": "서울가동초등학교",
  "passwordHash": "hashed-password",
  "status": "active",
  "createdAt": "timestamp",
  "updatedAt": "timestamp",
  "lastLoginAt": "timestamp"
}
```

### 9.2 `questionBanks`

문항 풀은 정적 JSON 파일로 둘 수 있다. Firestore로 이관할 경우 다음 구조를 사용한다.

```json
{
  "id": "teacher-0001",
  "audience": "teacher",
  "sourceSheet": "교원용",
  "sourceRow": 4,
  "area": "Ⅰ. 협력적 학교자치문화",
  "subarea": "Ⅰ-1. 소통과 협력의 학교자치 기반 조성",
  "indicator": "학교 비전 공유 및 실현",
  "question": "평가문항 텍스트"
}
```

### 9.3 `surveyDrafts`

```json
{
  "id": "auto-id",
  "schoolId": "school-id",
  "title": "2026학년도 1학기 학교교육과정 운영 평가 설문",
  "schoolName": "서울가동초등학교",
  "surveyDate": "2026-06-30",
  "introByAudience": {
    "teacher": "교원용 안내문",
    "parent": "학부모용 안내문",
    "student": "학생용 안내문",
    "staff": "교직원용 안내문"
  },
  "createdAt": "timestamp",
  "updatedAt": "timestamp",
  "googleForm": {
    "formId": "string",
    "editUrl": "string",
    "responderUrl": "string",
    "createdAt": "timestamp",
    "googleEmail": "user@example.com"
  }
}
```

### 9.4 `surveyDraftItems`

```json
{
  "id": "auto-id",
  "draftId": "draft-id",
  "audience": "teacher",
  "sourceQuestionId": "teacher-0001",
  "sourceRow": 4,
  "area": "영역",
  "subarea": "세부영역",
  "indicator": "평가지표",
  "originalQuestion": "원문 평가문항",
  "editedQuestion": "수정 평가문항",
  "responseType": "likert_5",
  "order": 1,
  "createdAt": "timestamp",
  "updatedAt": "timestamp"
}
```

### 9.5 `adminLogs`

```json
{
  "id": "auto-id",
  "action": "reset_school_password",
  "targetSchoolId": "school-id",
  "createdAt": "timestamp"
}
```

## 10. 기술 요구사항

### 10.1 프론트엔드

- GitHub/Vercel 배포에 적합한 Next.js 기반 구성을 권장한다.
- 화면 상태는 클라이언트 상태와 Firestore 저장 상태를 분리한다.
- 문항 목록은 대상/영역/세부영역/평가지표 기준으로 빠르게 필터링되어야 한다.

### 10.2 백엔드/API

- Vercel Serverless Functions 또는 Next.js Route Handlers 사용을 권장한다.
- 필요한 서버 기능:
  - 학교 등록
  - 로그인
  - 로그아웃
  - 비밀번호 초기화
  - DOCX 생성
  - Google OAuth callback 처리
  - Google Forms 생성

### 10.3 Firebase

- Firestore를 기본 DB로 사용한다.
- 학교 계정 비밀번호는 해시로 저장한다.
- 클라이언트에서 직접 민감 데이터를 수정하지 않도록 Firestore Security Rules를 설정한다.
- 관리자 작업은 서버 API를 통해서만 수행한다.

### 10.4 Google OAuth / Forms API

- Google Cloud Console에서 OAuth Client를 생성한다.
- Vercel 배포 URL과 로컬 개발 URL을 Authorized redirect URI에 등록한다.
- Google Forms API를 활성화한다.
- OAuth scope는 Forms 생성/수정에 필요한 최소 scope를 사용한다.
- Google 공식 문서 기준, Form 생성은 먼저 빈 Form을 만들고 이후 batch update로 문항을 추가하는 흐름이 필요하다.

### 10.5 DOCX 생성

- 서버에서 DOCX 바이너리를 생성해 다운로드 응답으로 반환한다.
- 파일명 예시:
  - `2026학년도_1학기_학교교육과정_운영_평가_설문_서울가동초등학교.docx`
- 문항 수가 많아도 페이지 나눔과 표 구조가 깨지지 않아야 한다.

## 11. 보안 요구사항

- 학교 비밀번호 원문 저장 금지
- 관리자 비밀번호 코드 하드코딩 금지, 환경변수 사용 권장
- 로그인 세션 쿠키는 HttpOnly, Secure, SameSite 옵션 적용
- Google OAuth token은 최소 저장 원칙 적용
- Firestore Security Rules로 학교별 데이터 접근 제한
- 관리자 API는 관리자 인증 후에만 실행
- 비밀번호 초기화 시 기존 세션 무효화 검토

## 12. 검증 기준

### 12.1 학교 계정

- 새 학교 등록이 가능하다.
- 중복 학교명 등록은 차단된다.
- 올바른 비밀번호로 로그인된다.
- 틀린 비밀번호로 로그인되지 않는다.
- 관리자가 학교 비밀번호를 초기화할 수 있다.

### 12.2 문항 선택

- 네 대상 탭이 표시된다.
- 영역, 세부영역, 평가지표 필터가 정상 동작한다.
- 문항 클릭 시 선택 목록에 추가된다.
- 선택 문항은 수정, 삭제, 순서 변경이 가능하다.
- 새로고침 후에도 초안이 유지된다.

### 12.3 출력

- 선택 문항을 포함한 DOCX 파일이 생성된다.
- DOCX에 학교명, 제목, 날짜, 대상별 섹션이 포함된다.
- 5점 척도 문항과 서술형 문항이 구분되어 출력된다.

### 12.4 Google Forms

- Google OAuth 로그인 후 앱으로 정상 복귀한다.
- Google Forms가 생성된다.
- 선택 문항이 Form에 추가된다.
- 생성된 편집 링크와 응답 링크가 화면에 표시된다.

## 13. MVP 범위

1. 학교 등록/로그인
2. 관리자 로그인 및 학교 비밀번호 초기화
3. JSON 문항 풀 로딩
4. 대상 탭별 필터링
5. 문항 담기, 수정, 삭제, 순서 변경
6. 학교별 설문 초안 저장
7. DOCX 다운로드
8. Google OAuth 연결
9. Google Forms 생성

## 14. 후속 개선 후보

- Google Forms 응답 수집 및 통계 대시보드
- 학교별 설문 템플릿 복제
- 문항 추천 기능
- Excel/PDF 신규 업로드 후 문항 풀 자동 추출
- 관리자 계정 다중화
- DOCX 외 PDF 직접 출력
- HWP 변환 지원
- 설문 결과 보고서 자동 생성

## 15. 외부 문서 참고

- Google Forms API `forms.create`: https://developers.google.com/workspace/forms/api/reference/rest/v1/forms/create
- Google Forms API `forms.batchUpdate`: https://developers.google.com/workspace/forms/api/reference/rest/v1/forms/batchUpdate
- Google OAuth 2.0 scopes: https://developers.google.com/identity/protocols/oauth2/scopes
